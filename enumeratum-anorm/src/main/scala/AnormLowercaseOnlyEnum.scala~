package enumeratum

/**
  * Provides lowercase instances for Anorm typeclasses:
  *
  * - [[anorm.Column]]
  */
trait AnormLowercaseOnlyEnum[A <: EnumEntry] { self: Enum[A] =>
  implicit def column: Column[A] =
    AnormColumn.lowercaseOnlyColumn[A](self)
}
